# Simple windows Nasm projects

# Description
Samples for free usage when combining multiple elements into one project.

# Includes

- MessageBox window + console print (messagebox_print.asm)
- Cmd calc.exe command executing (exec_cmd_calc.exe.asm)
- CreateProcessA start notepad.exe (CreateProcessA_notepad_exe.asm)
